pipeline {
    agent any
    stages {
        stage('Install Dependencies') {
            steps {
			    bat 'npm install'
            }
        }
        stage('Run WDIO Tests') {
            steps {
                bat 'npm run wdio'
            }
        }
    }
}